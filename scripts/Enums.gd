class_name Enums

enum Alliance { Red, Blue }
enum Goal { HighOuter, HighInner, Low }
enum Stage { One = 0, Two = 1, Three = 2 }
enum StageState { Locked, Balls, RotateColorWheel, RotateToColor, Complete }
enum GameMode { Auto, Operator, EndGame }
enum ColorWheelColor { Red, Green, Blue, Yellow }
